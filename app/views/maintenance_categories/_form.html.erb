<%= form_for(@maintenance_category) do |f| %>
  <% if @maintenance_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@maintenance_category.errors.count, "error") %> prohibited this maintenance_category from being saved:</h2>

      <ul>
      <% @maintenance_category.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
