<%= form_for(@car_category) do |f| %>
  <% if @car_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@car_category.errors.count, "error") %> prohibited this car_category from being saved:</h2>

      <ul>
      <% @car_category.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
